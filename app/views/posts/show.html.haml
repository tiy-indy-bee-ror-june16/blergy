%h1= @post.title
.content
  = simple_format @post.body
%p.meta Posted by #{@post.user.username}

- if current_user
  = render partial: 'addendums/form', locals: {object: @post}

#post_addendums
  = render @post.addendums
